# Folder Arsip (refrence/)

Berisi implementasi referensi awal sebelum penataan ke struktur bersih.
Kode aktif ada di `firmware/` dan `dashboard/`. Folder ini disimpan sebagai
arsip/pembanding dan tidak dipakai saat menjalankan sistem.
